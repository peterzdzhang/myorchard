@if (!Request.IsAjaxRequest())
{
    <a id="pager-current" href="#">@Model.Value</a>
}
else
{
    <button class="btn btn-primary" disabled="disabled" type="submit" name="page" value="@Model.Value">@Model.Value</button>
    <input type="hidden" name="page" value="@Model.Value" />
}