@if (!Request.IsAjaxRequest())
{
    <a id="pager-current" href="#">@Model.Value</a>
}
else
{
    <button class="btn btn-primary" disabled="true" type="submit" name="page" value="@Model.Value">@Model.Value</button>
}